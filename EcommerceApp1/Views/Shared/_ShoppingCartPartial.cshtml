@{
    Layout = null;
}

@if (ViewBag.ShoppingCart == null) {
    <div>Shopping Cart is null</div>
}
else
{
    @foreach (var cartItem in ViewBag.ShoppingCart)
    {
        <li class="list-row">
            <div class="text-xs uppercase font-semibold opacity-60">@cartItem.CartItemName</div>
            <div class="join">
                <button class="join-item btn btn-square btn-ghost">
                    <svg class="size-[1.2em]" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><g stroke-linejoin="round" stroke-linecap="round" stroke-width="2" fill="none" stroke="currentColor"><path d="M6 3L20 12 6 21 6 3z"></path></g></svg>
                </button>
                <span class="join-item">@cartItem.CartItemQuantity</span>
                <button class="join-item btn btn-square btn-ghost">
                    <svg class="size-[1.2em]" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><g stroke-linejoin="round" stroke-linecap="round" stroke-width="2" fill="none" stroke="currentColor"><path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z"></path></g></svg>
                </button>
            </div>
        </li>
    }
    @*<div
        tabindex="0"
        class="card card-compact dropdown-content bg-base-100 z-1 mt-3 w-52 shadow">
        <div class="card-body">
            <ul class="list bg-base-100 rounded-box shadow-md">
                @foreach (var cartItem in ViewBag.ShoppingCart)
                {
                    <li class="list-row">
                        <div class="text-xs uppercase font-semibold opacity-60">@cartItem.CartItemName</div>
                        <div class="join">
                            <button class="join-item btn btn-square btn-ghost">
                                <svg class="size-[1.2em]" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><g stroke-linejoin="round" stroke-linecap="round" stroke-width="2" fill="none" stroke="currentColor"><path d="M6 3L20 12 6 21 6 3z"></path></g></svg>
                            </button>
                            <span class="join-item">@cartItem.CartItemQuantity</span>
                            <button class="join-item btn btn-square btn-ghost">
                                <svg class="size-[1.2em]" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><g stroke-linejoin="round" stroke-linecap="round" stroke-width="2" fill="none" stroke="currentColor"><path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z"></path></g></svg>
                            </button>
                        </div>
                    </li>
                }
            </ul>
            <div class="card-actions">
                <button class="btn btn-primary btn-block">View cart</button>
            </div>
        </div>
    </div>*@
}